# The four detectors import only transformers and torch. Anything else that used
# to be listed here was never imported.
#
# `pip install -e .` reads pyproject.toml and is preferred; this file is kept so
# the documented `pip install -r requirements.txt` path still works.
transformers>=4.30.0
torch>=2.0.0
